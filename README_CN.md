[English Version](README.md)
# SSR systemd 安装脚本 (Debian 11)

本仓库提供 在 **Debian 11** 上 **基于systemd 的 ShadowsocksR (SSR)安装脚本**.

⚠️ 本项目服务于 **已理解自己配置和协议选择的SSR用户**.

------------------------------------------------------------------------

## 特性

-   用于 **Debian 11**
-   使用 **systemd** 用于服务管理
-   **不** 使用 `rc.local`
-   **不** 使用  `logrun.sh`
-   **不** 更改或生成配置文件
-   保持原有协议/混淆行为逻辑

本安装脚本仅用于 **环境配置**.

------------------------------------------------------------------------

## 本脚本做什么

 `install.sh` 脚本将:

1.  安装所需系统依赖 (`python2`, `git`等.)
2.  从我的备份仓库Clone.
3.  配置一个 `systemd` 服务
4.  将服务设为开机启动

**不会做**:

-   脚本结束后自动启动SSR服务
-   生成或修改任何JSON配置文件
-   更改任何协议/加密/混淆配置

------------------------------------------------------------------------

## 支持的系统

-   Debian GNU/Linux 11 (Bullseye)
-   systemd-based systems only

    其他分发版将**不被支持**.

------------------------------------------------------------------------

## 安装

Run as `root`:

``` bash
git clone https://github.com/GarrodRan-vdao/ssr-install-on-debian11
cd ssr-install-on-debian11
bash install.sh
```

结束后，SSR服务将 **enable** 但未 **start**.

------------------------------------------------------------------------

## 配置

本脚本不创建任何配置文件.

如果你发现仓库中 `user-config.json` 不存在, 可以使用原项目中的初始化脚本生成:

``` bash
cd /opt/shadowsocksr
bash initcfg.sh
```

然后搞他:

```
vim user-config.json
```

⚠️ 开启服务之前**务必仔细编辑&审核你的配置文件**
.

搬运一下doubi的配置教程: https://doubibackup.com/m_u379fq.html

------------------------------------------------------------------------

## 服务管理

开启 SSR:

``` bash
systemctl start ssr
```

检查状态:

``` bash
systemctl status ssr
```

查看日志:

``` bash
journalctl -u ssr -n 50 --no-pager
```

------------------------------------------------------------------------

## 相关项目

-   我的备份: https://github.com/GarrodRan-vdao/ssr-archive
-   👆从doubi拷过来的: https://github.com/ToyoDAdoubiBackup/shadowsocksr

------------------------------------------------------------------------

## 免责

本项目只用于 **存档与兼容适配**.

ShadowsocksR 已不再是一个有维护的项目. 使用风险和责任自负.

本安装脚本是为了使传统SSR部署在现代Debian系统上保持可用.